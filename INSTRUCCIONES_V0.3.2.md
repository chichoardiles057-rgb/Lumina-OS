# Lumina OS v0.3.2 — Corrección de importación

Reemplazá estos archivos:

```text
scripts/import_historical.py
.gitignore
```

Luego registrá el cambio:

```bash
git add .gitignore scripts/import_historical.py INSTRUCCIONES_V0.3.2.md
git commit -m "v0.3.2 - Corregir trazabilidad de importación"
git push
```
